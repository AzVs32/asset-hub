{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://asset-hub.local/schemas/plugin-manifest-v3.json",
  "title": "Asset Hub Plugin Manifest V3",
  "type": "object",
  "additionalProperties": false,
  "required": ["manifest_version", "plugin", "runtime", "permissions"],
  "properties": {
    "$schema": { "type": "string" },
    "manifest_version": { "const": 3 },
    "plugin": { "$ref": "#/$defs/plugin" },
    "runtime": { "$ref": "#/$defs/runtime" },
    "web": { "$ref": "#/$defs/web" },
    "capabilities": { "$ref": "#/$defs/capabilities" },
    "permissions": { "$ref": "#/$defs/permissions" }
  },
  "$defs": {
    "canonicalId": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9._:-]+$"
    },
    "pluginId": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9._-]+$"
    },
    "nonBlankString": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "plugin": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "version", "publisher"],
      "properties": {
        "id": { "$ref": "#/$defs/pluginId" },
        "name": { "$ref": "#/$defs/nonBlankString" },
        "version": { "$ref": "#/$defs/nonBlankString" },
        "publisher": { "$ref": "#/$defs/nonBlankString" },
        "description": { "type": "string" }
      }
    },
    "runtime": {
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["type"],
          "properties": { "type": { "const": "builtin" } }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["type", "wasm"],
          "properties": {
            "type": { "const": "extism" },
            "wasm": { "$ref": "#/$defs/nonBlankString" },
            "wasi": { "type": "boolean", "default": false },
            "plugin_api": {
              "type": "string",
              "const": "asset-hub.plugin-api@0.3",
              "default": "asset-hub.plugin-api@0.3"
            }
          }
        }
      ]
    },
    "web": {
      "type": "object",
      "additionalProperties": false,
      "required": ["root"],
      "properties": { "root": { "$ref": "#/$defs/nonBlankString" } }
    },
    "capabilities": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "kinds": { "type": "array", "items": { "$ref": "#/$defs/kind" }, "default": [] },
        "directory_kinds": { "type": "array", "items": { "$ref": "#/$defs/directoryKind" }, "default": [] },
        "actions": { "type": "array", "items": { "$ref": "#/$defs/action" }, "default": [] }
      }
    },
    "directoryKind": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind"],
      "properties": {
        "kind": { "$ref": "#/$defs/canonicalId" },
        "parent": { "$ref": "#/$defs/canonicalId" },
        "label": { "$ref": "#/$defs/nonBlankString" }
      }
    },
    "kind": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind"],
      "properties": {
        "kind": { "$ref": "#/$defs/canonicalId" },
        "parent": { "$ref": "#/$defs/canonicalId" },
        "label": { "$ref": "#/$defs/nonBlankString" },
        "supports_content": { "type": "boolean", "default": true },
        "detect": { "$ref": "#/$defs/contentMatcher" }
      }
    },
    "contentMatcher": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "mime_types": { "type": "array", "items": { "$ref": "#/$defs/nonBlankString" } },
        "extensions": { "type": "array", "items": { "$ref": "#/$defs/nonBlankString" } }
      }
    },
    "action": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "label", "handler", "views"],
      "properties": {
        "id": { "$ref": "#/$defs/canonicalId" },
        "label": { "$ref": "#/$defs/nonBlankString" },
        "handler": { "$ref": "#/$defs/canonicalId" },
        "applies_to": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "kinds": { "type": "array", "items": { "$ref": "#/$defs/canonicalId" } },
            "media_types": { "type": "array", "items": { "type": "string" } },
            "extensions": { "type": "array", "items": { "type": "string" } }
          }
        },
        "access": { "enum": ["read", "write"], "default": "read" },
        "requires": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "content": { "type": "boolean", "default": false },
            "content_delivery": { "enum": ["inline", "reference"] }
          }
        },
        "views": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "enum": ["text", "markdown", "html", "plugin_frame", "json", "media", "binary_url", "table", "form"] }
        },
        "ui": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "group": { "type": "string" },
            "order": { "type": "integer" },
            "locations": { "type": "array", "items": { "type": "string" } }
          }
        }
      }
    },
    "permissions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "allow": {
          "type": "array",
          "uniqueItems": true,
          "items": { "enum": ["resource.read", "resource.write", "content.read", "content.replace", "derived_asset.write"] }
        },
        "network": {
          "oneOf": [
            { "const": false },
            {
              "type": "object",
              "additionalProperties": false,
              "required": ["hosts"],
              "properties": { "hosts": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/nonBlankString" } } }
            }
          ]
        },
        "filesystem": {
          "oneOf": [
            { "const": false },
            {
              "type": "object",
              "additionalProperties": false,
              "required": ["read", "write"],
              "properties": {
                "read": { "type": "array", "items": { "$ref": "#/$defs/nonBlankString" } },
                "write": { "type": "array", "items": { "$ref": "#/$defs/nonBlankString" } }
              }
            }
          ]
        }
      }
    }
  }
}
