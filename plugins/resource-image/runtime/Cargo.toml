[package]
name = "resource-image-plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
asset-plugin-sdk = { path = "../../../asset-plugin-sdk", features = ["extism-guest"] }

[profile.release]
lto = true
opt-level = "z"
strip = true

[workspace]
